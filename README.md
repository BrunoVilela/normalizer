# Normalizer: making data normal again!

## Install and use

Install and load devtools:
```
install.packages(devtools)
library(devtools)
```
Install the package from github:
```
install_github("BrunoVilela/normalizer")
```
Load the package:
```
library(normalizer)
```
Check the help for more information:
```
?normalizer
```

